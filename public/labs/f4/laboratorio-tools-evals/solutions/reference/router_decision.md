# Decisión router tools

Estado: **publicar**.

- `route_accuracy`: `1.0`.
- `task_pass_rate`: `1.0`.
- `trace_complete_rate`: `1.0`.
- `tool_error_count`: `0`.

La decisión comprueba que cada pregunta use la ruta adecuada: RAG para documentos, SQL para datos, clasificador para etiquetas y código para cálculo exacto.
