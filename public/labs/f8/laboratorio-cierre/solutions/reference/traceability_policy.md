# Politica de trazabilidad de referencia

Cada evento usado para decision debe tener `trace_id`. El control `missing_trace_rate` solo cierra si vale `0.0`.

La evidencia esperada es un dataset corregido y un reporte regenerado con el mismo contrato.
