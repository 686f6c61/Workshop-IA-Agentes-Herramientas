# Entrega: de píxeles a patches

## Contexto

Describe qué imagen o captura quieres representar y qué tarea intentaría resolver el sistema.

## Ejecución

- Comando usado:
- Resultado de `make test`:
- Ficheros revisados:
  - `output/patch_report.md`
  - `output/patch_report.json`
  - `output/patch_grid.svg`

## Cambio realizado

Explica qué cambiaste en `data/resolution_cases.json`, `data/synthetic_ticket.ppm` o `contracts/patch_policy.json`.

## Evidencia

1. Tokens visuales antes/después:
2. Pares de atención antes/después:
3. Riesgo detectado: resolución, texto pequeño, aspect ratio, padding o coste.

## Decisión técnica

Decide si usarías más resolución, patches más pequeños, un preprocesado de OCR, recorte de región o una representación más barata.

## Límites

Qué no demuestra esta maqueta y qué medirías con un modelo visual real.
